<!-- 登录页 -->
<view class="page-wrap">
  <view class="login-logo">
    <text class="logo-circle">⭐</text>
    <text class="login-title">欢迎使用星语</text>
  </view>

  <view class="form-group">
    <text class="form-label">手机号</text>
    <input class="form-input" type="number" maxlength="11" placeholder="请输入手机号" value="{{phone}}" bindinput="onPhoneInput" />
  </view>

  <view class="form-group">
    <text class="form-label">验证码</text>
    <view class="input-row">
      <input class="form-input flex-1" type="number" maxlength="6" placeholder="请输入验证码" value="{{code}}" bindinput="onCodeInput" />
      <button class="btn btn-code" bindtap="sendCode" disabled="{{countdown > 0}}">
        {{countdown > 0 ? countdown + 's后重发' : '获取验证码'}}
      </button>
    </view>
  </view>

  <button class="btn btn-primary btn-block" bindtap="doLogin" loading="{{loading}}">登录</button>

  <view class="divider-text"><text>其他方式</text></view>

  <button class="btn btn-wechat btn-block" bindtap="doWechatLogin" loading="{{loading}}">
    <text>💬 微信一键登录</text>
  </button>

  <view class="form-bottom">
    <text>还没有账号？</text>
    <text class="link" bindtap="goRegister">立即注册</text>
  </view>
</view>
